# STASH — 保留 / 要ユーザー入力の項目

このファイルは、現時点で完了できない or ユーザー入力が必要なため**意図的に保留**した作業を記録します。
各項目には「なぜ保留か」と「再開時の手順」を明記します。

---

## (a) 実画像での認識精度の実測 — DEFERRED

**項目**: 実画像での認識精度の実測（90枚中80枚 = ≈89% を Playwright で確認）

**保留理由**:
画像認識（写真タブ）はユーザー自身の Vision API キー（Gemini / OpenAI / Anthropic）に依存します。
当リポジトリにはキーを同梱しておらず（プライバシー設計上、キーはユーザーのブラウザ `localStorage` のみに保存）、
**実 API キーが無いと実画像での認識を一度も走らせられない**ため、精度の実測は保留します。

**キー入手後の実行手順（Playwright）**:

1. **テスト画像を用意する**
   - 麻雀の局面（手牌が写った）スクリーンショット/写真をオンラインから収集する
     （例: 牌譜サイト・対局アプリのスクショ・牌姿画像）。
   - 各画像に対する **ground truth（正解の牌列、tiles.js 表記）** を手作業で書き出し、
     `test/fixtures/vision/<画像名>.json` 等に `{ "image": "...", "tiles": ["1m","2m",...] }` で保存する。
   - 牌の総数が 90 枚程度になるよう複数画像を集める（目標サンプル: 90 枚）。

2. **キーを設定する**
   - ローカルサーバーを起動: `python3 -m http.server 8000` → http://localhost:8000
   - 画面右上の **⚙ 設定** からプロバイダとキーを入力（`localStorage` に保存）。
   - Playwright からは `localStorage.setItem(...)` で直接注入してもよい
     （キー名・値の形式は `js/vision/recognize.js` の実装に合わせる）。

3. **写真タブに流して認識させる**
   - 各テスト画像を「写真で計算」タブの画像入力（`<input type="file">`）に
     Playwright の `setInputFiles` でアップロードする。
   - `recognizeTiles` の結果（認識牌チップ）を DOM から読み取る。

4. **認識 vs 正解 を突き合わせ、牌ごとの精度を計算する**
   - 画像ごとに「認識された牌列」と「ground truth」を比較。
   - **per-tile accuracy** = （正しく認識できた牌の総数）÷（ground truth の牌の総数）。
   - 全画像合算で「90 枚中 N 枚正解 = N/90」を算出し、≈89% 目標と比較する。
   - 結果を `test/` 配下にレポート出力（JSON or Markdown）。

**メモ**: プロバイダ／モデルによって精度は変動する。実測時は使用したプロバイダ・モデル名を
必ず記録すること（下記 (b) と関連）。

---

## (b) 要ユーザー入力 — 設計上の未確定事項

### b-1. 既定プロバイダ／モデルの選定
- 現在の既定は **Gemini**（SPEC 準拠）。ユーザーが他プロバイダ（OpenAI / Anthropic）を
  優先するか未確認。
- 各プロバイダで使う**具体的なモデル名**（例: Gemini の vision 対応モデル、OpenAI の
  vision 対応モデル、Anthropic の vision 対応モデル）はユーザーの契約・好みに依存。
- **再開時**: ユーザーに優先プロバイダと使用モデルを確認し、`js/vision/recognize.js` の
  既定値と ⚙ 設定 UI の選択肢を確定する。

### b-2. ローカル/ハウスルール（独自役）の扱い
- SPEC に列挙された標準役・役満は実装対象。一方で、卓ごとに異なる**ローカル役**
  （例: 人和の翻数扱い、燕返し・大車輪などのローカル役満、喰い替えの可否、
  切り上げ満貫の有無、青天井ルール等）の採否は未確定。
- **再開時**: ユーザーに採用したいローカル役・ハウスルールを確認し、
  `js/engine/` に有効/無効フラグとして実装するか判断する。
