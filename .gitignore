# macOS
.DS_Store

# Node (only if tooling is ever added; this is a no-build static site)
node_modules/

# Logs
*.log

# Editor
.vscode/

.playwright-mcp/
probe.mjs

# Secrets / local API keys (Vision testing) — never commit
.env.local
*.local
test/fixtures/vision/*.key
